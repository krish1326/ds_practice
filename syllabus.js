/**
 *  // Java script
 * 1. Hoisting. // done
 * 2. Scope : types of scopes we have in javascript // done
 * 3. environments : types we have.
 * 4 closures. // done 
 * 5 iife 
 * 6 this variable
 * 7 object oriented javascript.(objects creation methods and object properties)
 * 8. let vs const vs var and temporal dead zone.
 * 9. prototypal inheritance.
 * 10.garbage collection in javascript
 * 11. AOT vs JIT compilations
 * 12. es6 features : destructuring, arrow functions, spread and rest operators, template literal strings,classes
 * 13. eventLoop.
 * 14. setTimeout, setInterval, debouncing, throttling.
 * 15. modules and types in javascript.(AMD, commonjs, es6 modules).
 * 16. pollyfills and shims.
 * 17. babel, typescript, transpilation, webpack working
 * 18. web API's ==> local storage,  session storage, 
 * 19. cookies, jwt
 * 20. xmlHTTPRequest what is the protocol used under the hood.
 * 21. async await , promises (and it's all updated methods)
 * 22. http methods.
 * 23 error codes (30x, 40x, 50x).
 * 24. typeof null/undefined quirks with these. 
 * 
 * 
 * // design patterns: 
 * 1. observer design pattern
 * 2. singleton design pattern
 * 3. facade design pattern.
 * 4. publish subscribe design pattern.
 * 5. stratagey design pattern.
 * 
 * // ds and algorithms :
 * 
 * 1. array manupulations(numbers and strings),
 * 2. stack.
 * 3. queue.
 * 4. binary search.( square root problem asked in interview )
 * 
 *  ** Questions asked in ds and algo previously**
 * 
 *  1. find square root of a number to a certain precision.
 *  2. flat an array(of any level deep) and find sum of squares.
 *  3. prime numbers in a given interval.
 *  4. frequency count of number of occurences.
 *  5. maximum subarray, contigous.
 *  6. linked List.
 * 
 * // css.
 * 1. position: absolute, relative, static, (relative parent absolute )
 * 2. display : block, table, inline , inline-block
 * 3. grid
 * 4. flexbox.
 * 5. responsive web design.
 * 6. clip.
 * 7. preprocessor --> why do we need it ? and types saas, less
 * 8. box model.
 * 9. what is cascading in css. and types of styling(inline, separate link, link)
 * 10. specificity order of css.
 * 
 * // HTML5
 * 
 * 1. media API's(audio and video)
 * 2. meta tags (like device-width='1.0', manifest.json)
 * 3. pre tag.
 * 4. canvas api.
 * 5. what is html
 * 6. semantics.
 * 
 */